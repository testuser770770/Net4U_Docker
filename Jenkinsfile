pipeline{
    agent any
    stages {
        stage ('checkout'){
            steps{
                checkout scm: [
                    sh 'echo "checking out repo"'

                ]
            }
        }
    }
}